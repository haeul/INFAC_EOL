<?xml version="1.0" encoding="utf-8" ?>
<configuration>
	<configSections>
		<section name="log4net" type="log4net.Config.Log4NetConfigurationSectionHandler, log4net"/>
	</configSections>

	<log4net>
		<!-- 날짜 단위로 Rolling 하는 파일 Appender -->
		<appender name="system_logger" type="log4net.Appender.RollingFileAppender">
			<!-- 로그 파일 이름의 선두 부분 -->
			<File value="./Log/sys/"/>
			<!-- 기존에 로그 파일이 있다면 연결해서 사용 여부 -->
			<appendToFile value="true"/>
			<!-- 날짜 마다 파일을 작성하는 것을 지정 -->
			<rollingStyle value="date"/>
			<!-- 로그 파일 이름 고정 여부. 고정하지 않을 때는 false -->
			<staticLogFileName value="false"/>
			<!-- 파일 이름에의 날짜 부분 -->
			<datepattern value="yyyy\\\\yyyy-MM\\\\yyyy-MM-dd'_System.log'"/>
			<layout type="log4net.Layout.PatternLayout">
				<!-- 로그 형식 -->
				<!-- <conversionPattern value="%date{yyyy-MM-dd HH:mm:sss.fff} [%thread] %-5level %logger - %message%newline" /> -->
				<conversionPattern value="%date{yyyy-MM-dd HH:mm:sss.fff} [%-5level]  %message%newline"/>
			</layout>
		</appender>

		<!-- 날짜 단위로 Rolling 하는 파일 Appender -->
		<appender name="ch1_logger" type="log4net.Appender.RollingFileAppender">
			<!-- 로그 파일 이름의 선두 부분 -->
			<File value="./Log/ch1/"/>
			<!-- 기존에 로그 파일이 있다면 연결해서 사용 여부 -->
			<appendToFile value="true"/>
			<!-- 날짜 마다 파일을 작성하는 것을 지정 -->
			<rollingStyle value="date"/>
			<!-- 로그 파일 이름 고정 여부. 고정하지 않을 때는 false -->
			<staticLogFileName value="false"/>
			<!-- 파일 이름에의 날짜 부분 -->
			<datepattern value="yyyy\\\\yyyy-MM\\\\yyyy-MM-dd'_System.log'"/>
			<layout type="log4net.Layout.PatternLayout">
				<!-- 로그 형식 -->
				<!-- <conversionPattern value="%date{yyyy-MM-dd HH:mm:sss.fff} [%thread] %-5level %logger - %message%newline" /> -->
				<conversionPattern value="%date{yyyy-MM-dd HH:mm:sss.fff} [%-5level]  %message%newline"/>
			</layout>
		</appender>

		<!-- 날짜 단위로 Rolling 하는 파일 Appender -->
		<appender name="ch2_logger" type="log4net.Appender.RollingFileAppender">
			<!-- 로그 파일 이름의 선두 부분 -->
			<File value="./Log/ch2/"/>
			<!-- 기존에 로그 파일이 있다면 연결해서 사용 여부 -->
			<appendToFile value="true"/>
			<!-- 날짜 마다 파일을 작성하는 것을 지정 -->
			<rollingStyle value="date"/>
			<!-- 로그 파일 이름 고정 여부. 고정하지 않을 때는 false -->
			<staticLogFileName value="false"/>
			<!-- 파일 이름에의 날짜 부분 -->
			<datepattern value="yyyy\\\\yyyy-MM\\\\yyyy-MM-dd'_System.log'"/>
			<layout type="log4net.Layout.PatternLayout">
				<!-- 로그 형식 -->
				<!-- <conversionPattern value="%date{yyyy-MM-dd HH:mm:sss.fff} [%thread] %-5level %logger - %message%newline" /> -->
				<conversionPattern value="%date{yyyy-MM-dd HH:mm:sss.fff} [%-5level]  %message%newline"/>
			</layout>
		</appender>

		<!-- 날짜 단위로 Rolling 하는 파일 Appender -->
		<appender name="ch1_capa_data" type="log4net.Appender.RollingFileAppender">
			<!-- 로그 파일 이름의 선두 부분 -->
			<File value="./CapaData/ch1/"/>
			<!-- 기존에 로그 파일이 있다면 연결해서 사용 여부 -->
			<appendToFile value="true"/>
			<!-- 날짜 마다 파일을 작성하는 것을 지정 -->
			<rollingStyle value="date"/>
			<!-- 로그 파일 이름 고정 여부. 고정하지 않을 때는 false -->
			<staticLogFileName value="false"/>
			<!-- 파일 이름에의 날짜 부분 -->
			<datepattern value="yyyy\\\\yyyy-MM\\\\yyyy-MM-dd'_ch1_capa_data.asc'"/>
			<layout type="log4net.Layout.PatternLayout">
				<!-- 로그 형식 -->
				<!-- <conversionPattern value="%date{yyyy-MM-dd HH:mm:sss.fff} [%thread] %-5level %logger - %message%newline" /> -->
				<conversionPattern value="%message%newline"/>
			</layout>
		</appender>
		
		<!-- 날짜 단위로 Rolling 하는 파일 Appender -->
		<appender name="ch2_capa_data" type="log4net.Appender.RollingFileAppender">
			<!-- 로그 파일 이름의 선두 부분 -->
			<File value="./CapaData/ch2/"/>
			<!-- 기존에 로그 파일이 있다면 연결해서 사용 여부 -->
			<appendToFile value="true"/>
			<!-- 날짜 마다 파일을 작성하는 것을 지정 -->
			<rollingStyle value="date"/>
			<!-- 로그 파일 이름 고정 여부. 고정하지 않을 때는 false -->
			<staticLogFileName value="false"/>
			<!-- 파일 이름에의 날짜 부분 -->
			<datepattern value="yyyy\\\\yyyy-MM\\\\yyyy-MM-dd'_ch2_capa_data.asc'"/>
			<layout type="log4net.Layout.PatternLayout">
				<!-- 로그 형식 -->
				<!-- <conversionPattern value="%date{yyyy-MM-dd HH:mm:sss.fff} [%thread] %-5level %logger - %message%newline" /> -->
				<conversionPattern value="%message%newline"/>
			</layout>
		</appender>

		<root>
			<!-- 로그 레벨 지정. 아래에서는 INFO 레벨 이상만 로그가 남는다 -->
			<level value="ALL"/>
			<!-- 아래 로그 레벨 중 하나 선택
			<level value="OFF " />
			<level value="FATAL" />
			<level value="ERROR" />
			<level value="WARN" />
			<level value="INFO" />
			<level value="DEBUG" />
			<level value="ALL" />
			-->

			<!-- 어느 로그를 사용할 것인지 지정한다. -->
			<!-- <appender-ref ref="system_logger"/> -->
			<!-- 복수 지정 가능 -->
		</root>

		<logger name="SystemLogger">
			<level value="ALL" />
			<appender-ref ref="system_logger" />
		</logger>

		<logger name="LoggerCh1">
			<level value="ALL" />
			<appender-ref ref="ch1_logger" />
		</logger>

		<logger name="LoggerCh2">
			<level value="ALL" />
			<appender-ref ref="ch2_logger" />
		</logger>

		<logger name="CapaDataCh1">
			<level value="ALL" />
			<appender-ref ref="ch1_capa_data" />
		</logger>

		<logger name="CapaDataCh2">
			<level value="ALL" />
			<appender-ref ref="ch2_capa_data" />
		</logger>

	</log4net>

	<startup>
		<supportedRuntime version="v4.0" sku=".NETFramework,Version=v4.8" />
	</startup>

	<!-- HiDPI 지원-->
	<System.Windows.Forms.ApplicationConfigurationSection>
		<add key="DpiAwareness" value="PerMonitorV2" />
	</System.Windows.Forms.ApplicationConfigurationSection>
</configuration>